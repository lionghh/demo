require(["ipu", "jquery", "iScroll", 'common'], function (ipu, $, iScroll, common) {
    $(function () {
        var navBar1 = ipu.navBar(".ipu-navbar", {
            animate: true
        });
    });

});
