require(['jquery', 'ipu', 'iScroll'], function ($, ipu, iScroll) {
    $(".todo").click(function () {
        ipu.toast('暂未实现');
    });
	
	$("body").on("touchstart", function () {
       // 让ios点击效果生效
    });
});